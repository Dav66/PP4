{%extends "base.html.twig"%}
{% block body %}
    {% if is_granted('IS_AUTHENTICATED_FULLY')%}
        <p>Username : {{app.user.username}}</p>
    {%else%}
       
        <p style="text-align: center">Vous n etes pas conecter</p>
    {%endif%}
    <div class="container" style="background-color: rgba(255,255,255,0.1)!important">
 
</div>
{% endblock body %}

{% block bouton %}
    <a class="nav-link float-right" href="/Ajouter">S'inscrire</a>
    {% if not is_granted ('IS_AUTHENTICATED_FULLY')%}
        <a class="nav-link float-right" href="{{path("login")}}">Se connecter</a>
        
    {% else %}
        <a class="nav-link float-right" href="{{path("logout")}}">Se deconnecter</a>
    {% endif %}
    {% endblock bouton %}
